<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
  <shortcut
    android:shortcutId="sessions"
    android:enabled="true"
	android:shortcutShortLabel="@string/home_shortcut"
    android:shortcutLongLabel="@string/home_shortcut"
    android:icon="@drawable/icon">
    <intent
      android:action="android.intent.action.VIEW"
      android:targetPackage="referenceguide.Droid"
      android:targetClass="referenceguide.Droid.MainActivity"
      android:data="sessions" />
    <!-- If your shortcut is associated with multiple intents, include them
         here. The last intent in the list is what the user sees when they
         launch this shortcut. -->
    <categories android:name="android.shortcut.conversation" />
  </shortcut>
  <!-- Specify more shortcuts here. -->
</shortcuts>
